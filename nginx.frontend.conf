server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: any unmatched path returns index.html so client-side routing works
  location / {
    try_files $uri $uri/ /index.html;
  }
}
